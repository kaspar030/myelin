[package]
name = "chanapi-testing"
version = "0.1.0"
edition = "2024"

[dependencies]
chanapi = { path = "../chanapi", features = ["tokio"] }
testing-service = { path = "../testing-service", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
