[package]
name = "myelin-testing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
myelin = { path = "../myelin", features = ["tokio"] }
testing-service = { path = "../testing-service", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
