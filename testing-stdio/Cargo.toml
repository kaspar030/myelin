[package]
name = "testing-stdio"
version = "0.1.0"
edition = "2024"

[dependencies]
chanapi = { path = "../chanapi", features = ["postcard"] }
testing-service = { path = "../testing-service", features = ["serde"] }
